## Packages
framer-motion | For smooth animations (entry, hover, layout changes)
lucide-react | Beautiful icons (already in base, but confirming usage)
date-fns | Formatting timestamps in chat

## Notes
- Tailwind config should extend font family to include 'Outfit' and 'DM Sans'
- Using localStorage to persist `participantId` per group
- Polling implementation for real-time chat feel using useQuery `refetchInterval`
